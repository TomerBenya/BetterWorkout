<h2>Templates</h2>

<div>

  {{#each templates}}
    <h3>{{name}}</h3>
    {{#each exerciseTemplates}}
      <p>{{add 1 @index}}
        -
        {{name}}
        {{sets}}
        X
        {{reps}}
        Increment:
        {{increment}}
        lbs</p>
    {{/each}}

  {{/each}}

</div>